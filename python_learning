Day 1:
python环境安装：网站：python.org;勾选“Add python to PATH”;自定义安装;disable path length limit;命令行输入python验证安装完成
git安装
开发工具——pycharm

输入：input.  a=input("提示词")  a为str
输出：print("")   输出变量值：print(f"{变量名}")  默认自带回车(回车→print())，无回车end=""(对齐用制表符"\t")
运算：//整除，%取余，**幂指数，and/or/not

快捷键：ctrl+d 快速复制一行   alt+shift+鼠标拖动 列编辑模式

Day 2:
if————if 条件: ……elif 条件: ……else: ……
模式匹配match————match 变量名:  case 对应模式: case _:（表示其他情况）
while————while 条件:循环体  else: ……
轮询遍历:for循环————for 元素 in ……:循环体 else:……
生成数字序列range————range(end)[从0开始];range(start,end,[step])[都不包含end]
嵌套循环————外循环+内循环

随机数random————import random; random.randint(strat,end)[包含start和end]

解包unpacking：将列表/元组内元素拆解赋值给多个变量      a,b,c,d=(5,10,8,9)/a,*b,c=(5,10,8,9)[b=[10,8]列表]

数据容器:（方法：s.方法名）sum()  len()  min/max()  '' in s  type(s)查看数据类型   s={/[i表达式 for i in …… if ……]/}
  列表list  s=[可重复、可修改、存放多类]  [del] s[i/-i] s[start:end:[step负数从后往前]]不包含end   列表合并:s1+s2
          方法：append(x)尾部插入,insert(index,x)索引前插入元素,remove(x)删除第一个匹配的元素,pop(index)删除对应索引元素[未指定-删除最后一个],sort()同类数据排序,reverse()反转
  字符串str  s="不可变"   方法：find()找子串位置,count()统计字符次数,upper/lower()转换大小写,split('分隔符')按分隔符分割成列表,strip()去除两端空白或指定字符,replace('子串1','子串2')替换子串,startswith/endswith()是否以子串开头/结尾
  元组tuple  s=(存放多类、不可修改)  count()统计元素数量,index()找元素位置  定义：s=()/(100,)[单个元素加',']
  
Day 3:
  集合set  s={无序(不可用索引)，不重复，可修改}  定义:set()   方法:add(x),remove(x),a=s.pop()[随机删除元素并返回],clear()清空,s1.difference(s2)[s1与s2的差集(在s1不在s2)='-'],union()并集[='|'],intersection交集[='&']
  字典dict s={key:value(key不重复),可修改,无索引}  定义:{}/dict()  方法:(增/改)dict[key]=value;(删)a=dict.pop[key],del dict[key];(查)value--dict[key]/dict.get(key);所有key/value/键值对--dict.keys()/values()/items()
Day 4:
  函数: def 函数名(参数):函数体(return 返回值)   """函数说明文档：功能、参数、返回值"""--help(函数名)可以查看文档   不定长参数:*args   匿名函数:num=lambda x,y:x+y
  保留小数:round(x,num)[num表示保留几位小数]
  文件读写:   睡眠延时:time.sleep(num单位:秒)
      打开文件:f=open(name[r'电脑原路径'],mode['r'只读,'w'写入,'a'追加(w和a可以创建新文件)],encoding="UTF-8")  
      读取文件:f.read([num]读取长度,默认读取全部长度);f.readlines()整个文件按行进行读取，输出列表，一行一元素;readline()一次读取一行，下次读取接在上次读取位置之后;for line in f:得到每行数据
      写入/追加写入文件:f.write(内容)→f.flush()  追加写入：'\n'换行
      关闭文件:f.close()[带有flush功能]
      打开文件后自动关闭: with open(name,mode,encoding) as f: 文件操作
  异常捕获: try:可能异常的代码 except [error名称,多个(eroor1,error2)] [as e]:触发异常之后代码 [else:没有异常时的代码] [finally:有无异常都执行的代码] [Exception表示全部异常]
Day 5:
  类: class 类名:  def __init__/方法名(self,形参列表):self.属性名=参数值  ;c=类名()  c.方法名()
  大语言模型(LLM):
      部署:本地/官方开放API(Application Programming Interface应用程序编程接口)/云服务平台
        本地部署:Ollama软件(ollama run 模型名称[在ollama的models去搜索])     API--API Key
  网络:IP地址定位设备(一般IPv4,32位二进制),127.0.0.1本机IP地址；domain name域名--DNS域名解析服务器；port端口号0-65535,标识运行程序[http协议默认端口80，https协议默认端443]
